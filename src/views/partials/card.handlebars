{{#each products}}
  {{!-- <div class="card" style="width: 18rem;">
    <img src="{{img}}" class="card-img-top" alt="{{title}}">
    <div class="card-body">
        <h5 class="card-title">{{title}}</h5>
        <h6>Descripción del producto:</h6>
        <p class="card-text">{{description}}</p>
        <p class="card-text">$ {{price}}</p>
        <p class="card-text">Stock: {{stock}}</p>
        <p class="card-text">Categoría: {{category}}</p>
        <p class="card-text">Código: {{code}}</p>
        <div>{{thumbnails}}</div>
        <a class="btn btn-primary">Agregar al carrito</a>
    </div>
</div> --}}
  <div class="customCardContainer">
    <div class="card customCard" style="width: 18rem;">
      <img src="{{img}}" class="card-img-top" alt="{{title}}" />
      <div class="card-body">
        <h5 class="card-title">{{title}}</h5>
        <p class="card-text">{{price}}</p>
        <p class="card-text">{{description}}</p>
        <p class="card-text">{{stock}}</p>
        <a  href="#" class="btn btn-primary">Agregar al
          carrito</a>
          <button class="btn btn-primary" onclick="api/products/${_id}">Agregar botón</button>
      </div>
    </div>
  </div>
{{/each}}