paths:
  /api/carts:
    post:
      summary: Crear un nuevo carrito
      tags:
      - Carts
      requestBody:
      


  /api/carts/{uid}:
    get:
      summary: Obtiene un carrito por ID
      tags: 
        - Carts
      parameters:
      - name: uid
        in: path
        required: true
        description: Id del carrito que se quire solicitar
        schema:
          $type: String
      responses: 
        "200":
          description: Carrito encontrado
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#components/schemas/Cart"
        "500":
          description: Error interno del servidor     

  ##/api/carts/{uid}:
  ##Vaciar el carrito
    delete:

  ##/api/carts/{uid}: 
    post:
  


  





components: 
  schemas: 
    Cart:
      type: object
      properties:
        _id:
          type: ObjectId
          description: ID autogenerado por MongoDb
        products: 
          type: ObjectId
          description: Listado de productos
          required: true
        quantity:
          type: Number
          description: Cantidad de productos en el carrito
          required: true


  requestBodies: 
    createCart:
      type: object
      properties:
        products: 
          type: ObjectId
          description: Lista de productos
        quantity: 
          type: Number
          description: Cantidad total de productos en el carrito
      example: 



        